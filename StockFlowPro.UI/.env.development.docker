# Docker Development Configuration
# Use this when running the full stack with docker-compose
# Use relative URLs since nginx proxy handles the routing
VITE_API_BASE_URL=/api
VITE_WS_URL=ws://localhost:3000
VITE_NODE_ENV=development