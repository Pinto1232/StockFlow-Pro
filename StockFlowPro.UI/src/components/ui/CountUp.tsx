// Deprecated shim for CountUp component. Prefer importing from barrel: { CountUp } from './ui'.
// Kept temporarily to avoid module resolution conflicts until file can be deleted.
export { CountUp } from './CountUp/CountUp';
